[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "sales-support-agent"
version = "0.1.0"
requires-python = ">=3.12,<3.15"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "requests",
  "PyJWT[crypto]>=2.8,<3.0",
  "SQLAlchemy>=2.0,<3.0",
  "psycopg2-binary",
  "google-auth",
  "python-multipart",
  "anthropic>=0.40.0",
  "markdown",
  "openpyxl>=3.1,<4.0",
  "python-docx>=1.1,<2.0",
  "pdfplumber>=0.11,<1.0",
  "Pillow>=10.0,<13.0",
  "tzdata>=2025.2",
]

[project.scripts]
app = "sales_support_agent.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["sales_support_agent*"]
